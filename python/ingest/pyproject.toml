[project]
name = "aural_ingest"
version = "0.0.0"
requires-python = ">=3.11"

[project.scripts]
aural_ingest = "aural_ingest.cli:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]

[tool.ruff]
line-length = 100
