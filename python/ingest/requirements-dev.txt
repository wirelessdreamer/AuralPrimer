pytest==8.3.4
pytest-benchmark==5.1.0
pytest-cov==6.0.0
ruff==0.8.4
# numpy is needed to even collect the pure-logic tests (many import it); the
# heavier transcription runtime (librosa/torch/basic-pitch) stays out of the
# lightweight lint-test job -- conftest.py skip-collects those suites.
numpy<2
