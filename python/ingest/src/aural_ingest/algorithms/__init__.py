"""Drum and melodic transcription algorithm implementations.

The implementations are lightweight and dependency-free so they can run inside
the sidecar runtime while still producing waveform-driven outputs.
"""
