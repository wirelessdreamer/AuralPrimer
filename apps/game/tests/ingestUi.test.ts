import { describe, it } from "vitest";

// Ingest form helpers moved to AuralStudio. The corresponding tests live
// alongside `apps/desktop/src/ingestUi.ts`. This stub keeps the file present
// for historical context without producing failing assertions.

describe.skip("apps/game ingestUi (moved to AuralStudio)", () => {
  it("intentionally skipped: see apps/desktop/tests/ingestUi.test.ts", () => {});
});
