import { describe, it } from "vitest";

// Ingest client moved to AuralStudio. The corresponding tests live alongside
// `apps/desktop/src/ingestClient.ts`. This stub keeps the file present for
// historical context without producing failing assertions.

describe.skip("apps/game ingestClient (moved to AuralStudio)", () => {
  it("intentionally skipped: see apps/desktop/tests/ingestClient.test.ts", () => {});
});
