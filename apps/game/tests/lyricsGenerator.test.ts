import { describe, it } from "vitest";

// Lyrics generation (creating features/lyrics.json from plain text) moved to
// AuralStudio. The corresponding tests live in `apps/desktop/tests`. This
// stub keeps the file present for historical context without producing
// failing assertions.

describe.skip("apps/game lyricsGenerator (moved to AuralStudio)", () => {
  it("intentionally skipped: see apps/desktop/tests/lyricsGenerator.test.ts", () => {});
});
