// Ingest sidecar client moved to AuralStudio (apps/desktop/src/ingestClient.ts).
// AuralPrimer (the gameplay app) does not run import flows. See `spec.md`
// section 1.1 for the app-role boundary. This stub remains only to keep
// historical imports from breaking until the next cleanup pass; it
// intentionally exports nothing.

export {};
