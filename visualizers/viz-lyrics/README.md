# viz-lyrics (built-in)

Karaoke-style lyrics renderer for AuralPrimer.

This visualizer consumes `VizInitContext.song.lyrics` (loaded from `features/lyrics.json`) and renders:
- the current line
- a highlight sweep based on per-chunk timings (word/syllable)

